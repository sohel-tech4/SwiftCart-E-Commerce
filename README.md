# 📘 JavaScript Questions & Answers

**Answer:**

1) What is the difference between null and undefined?
Answer : যেই ভ্যালুটা সঙ্গায়িত করা হয় না সেটা হলো undefined আর যেই ভ্যালুটা ইচ্ছে করে দেয়া হয় না সেটা null
2) What is the use of the map() function in JavaScript? How is it different from forEach()?
Answer : map() function একটি এ্যারে থাকলে সবগুলোর উপর কাজ করে এবং একটি এ্যারে রিটার্ন করে আর অন্য দিকে forEach() একটি এ্যারে থাকলে সবগুলোর উপর কাজ করে কিন্তু নির্দিষ্ট সংখ্যা জিনিস সে রেটার্ন করে। 
3) What is the difference between == and ===?
Answer : ==  শুধু ভ্যালু চেক করে আর অন্যদিকে === ভ্যালু + টাইপ দুইটাই চেক করে থাকে।
4) What is the significance of async/await in fetching API data?
Answer : একটি ফাংশনে দেখা যায় একটি এপিআইকে যখন কল করা হয় তখন কিছুটা সময় লাগে কিন্তু JavaScript সেই কাজটি না করে তার নেক্সট কাজটি করে ফেলে। কিন্তু আমরা যদি async/await ব্যবহার করলে সে ধাপে ধাপে কাজ গুলো সমাধান করবে।
5) Explain the concept of Scope in JavaScript (Global, Function, Block).
Answer : 
Global ‍Scope : যে কোন জায়গা থেকে এ্যাক্সেস করা যায়।
Function ‍Scope : শুধু মাত্র ফাংশনের মধ্য থেকে এ্যাক্সেস করা যায়।
Block Scope : let এবং const block scope follow করে ({} এর ভিতরে সীমাবদ্ধ)।